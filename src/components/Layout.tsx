import { Outlet } from "react-router-dom";

const Layout = () => {
    return (
        <div className="app">
            <div>Coming soon...</div>
            <Outlet />
        </div>
    );
};

export default Layout;
